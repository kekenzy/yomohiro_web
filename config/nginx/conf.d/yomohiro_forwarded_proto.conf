# Cloudflare 等の手前が HTTPS・オリジンへの接続が HTTP のとき、
# $scheme だけだと Django が「非セキュア」と誤認する。
# 上流が付けた X-Forwarded-Proto を優先し、無いときだけ $scheme を使う。
map $http_x_forwarded_proto $yomohiro_forwarded_proto {
    ""      $scheme;
    default $http_x_forwarded_proto;
}
